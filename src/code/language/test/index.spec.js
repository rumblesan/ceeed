/* global describe, it */

import assert from 'assert';

describe('Language', function () {
  it("doesn't have many tests currently", function () {
    const expected = 1;

    assert.equal(0, expected);
  });
});
